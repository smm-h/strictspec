// strictspec — TypeScript runtime library and CLI stub.
//
// Placeholder skeleton. The runtime (lossless JSON/TOML/JSONL parsers producing
// tagged document-model values, diagnostics, constraint engine) and the CLI
// launcher stub land in a later phase; see ts/DESIGN.md.

export const version = "0.0.0";
