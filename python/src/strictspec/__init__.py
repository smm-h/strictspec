"""strictspec — Python runtime library and CLI stub.

Placeholder skeleton. The runtime (document I/O, diagnostics, tagged values,
constraint engine) and the CLI launcher stub are implemented in a later phase.
"""

__version__ = "0.0.0"
