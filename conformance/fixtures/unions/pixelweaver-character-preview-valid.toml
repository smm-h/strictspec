name = "pixelweaver-character-preview-valid"
category = "unions"
provenance = "derived-from(examples/pixelweaver/samples/character-preview.valid.json, verified against spec)"
description = "PixelWeaver character-preview (the acceptance-test schema source): nested maps, discriminated oscillator-function unions, nullable paletteClassId. Validates clean."
schema = "_schemas/pixelweaver-character-preview.toml"
input = "_inputs/pixelweaver/character-preview.valid.json"

[expected]
valid = true
