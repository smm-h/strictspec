[migration]
schema = "AgentDefinition"
from_format_version = 1
to_format_version = 2
migration_set = "agent-budget"
description = "max_cost_usd -> cost_thresholds; rename_field + wrap_in_array."
[[ops]]
op = "rename_field"
from = "$.budget.max_cost_usd"
to = "$.budget.cost_thresholds"
down = "total"
[[ops]]
op = "wrap_in_array"
path = "$.budget.cost_thresholds"
down = "total"
[[down_ops]]
op = "unwrap_singleton"
path = "$.budget.cost_thresholds"
[[down_ops]]
op = "rename_field"
from = "$.budget.cost_thresholds"
to = "$.budget.max_cost_usd"
