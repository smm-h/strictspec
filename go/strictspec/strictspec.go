// Package strictspec is the placeholder root of the strictspec Go module:
// the toolchain (generator, interpreter, migration engine, constraint engine,
// diff engine, CLI) plus the Go runtime. This skeleton is fleshed out in later
// phases; see go/DESIGN.md.
package strictspec

// Version is the strictspec release version this module was built at.
const Version = "0.0.0"
