# META-SCHEMA REJECTION FIXTURE.
# An opaque JSON leaf that declares NEITHER `unchecked` NOR `consumer_check`.
# Validating this SCHEMA (a document of the meta-schema) must hard-error:
#   STRICTSPEC_SCHEMA_OPAQUE_NO_STANCE · path $.types.ToolSchema.fields.input_schema
# (decision 29). Every opaque leaf must declare its strictspec-blind stance on record.

[schema]
name = "AgentDefinition"
meta_version = 1
format_version = 1
document_syntax = "json"
description = "Rejection fixture — opaque leaf with no stance."

[types.ToolSchema]
kind = "record"
[types.ToolSchema.fields.input_schema]
type = "opaque"
required = false
# NO `unchecked` and NO `consumer_check` — this is the meta-schema violation.
